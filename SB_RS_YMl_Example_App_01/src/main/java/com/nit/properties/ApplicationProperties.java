package com.nit.properties;

/*import java.util.HashMap;
import java.util.Map;

import org.springframework.boot.autoconfigure.EnableAutoConfiguration;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Configuration;

@Configuration
@EnableAutoConfiguration
@ConfigurationProperties(prefix = "app")
public class ApplicationProperties {

       //If we are using @Value then no need to write this properties class
        * 
        * if we ant to get more no of msgs then we will this properties class
        * instead of using @Value annotation
        */
       
	/*private Map<String,String> messages = new HashMap<>();

	public Map<String, String> getMsgs() {
		return messages;
	}

	public void setMsgs(Map<String, String> msgs) {
		this.messages = msgs;
	}
	
	
}*/
